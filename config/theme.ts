/**
 * Design tokens – Green theme (DESIGN_REACTNATIVE.md §4.1)
 * Spacing, BorderRadius (Colors/Fonts ở constants/theme.ts)
 */

export const Spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
} as const;

export const BorderRadius = {
  sm: 8,
  md: 12,
  lg: 16,
  full: 9999,
} as const;
